repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: bash -c 'uv run --extra dev pytest tests/ -v'
        language: system
        pass_filenames: false
        always_run: true
